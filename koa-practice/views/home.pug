extends layout
block title
    h1 Home
block content
    h1 Current questions
    div(class='row marketing')
        for question in questions
            div(class='col-lg-6')
                h4 #{question.title}
                a(href='/vote?questionId=#{question._id.toString()}', class='btn btn-info btn-xs', role='button') Vote here
                a(href='/question/#{question._id.toString()}', class='btn btn-info btn-xs', role='button') Update here
    br
    a(href='/question', class='btn btn-primary btn-lg', role='button') Create a new question
